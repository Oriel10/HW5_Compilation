// simple test


byte foo(byte x, byte y){
    byte z;
    z = x + y;
    byte w = x;
    byte a = w;
    //return x + y;
}

void main()
{
    //byte x=2b;
    //byte y=3b;
    //byte z = foo(x,y);
    //int w = (int) z;
    //print("w = ");
    //printi(w);

    byte id = 200b;
    int f;
    f = id;
    printi(f);
    return;
}